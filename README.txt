Commands on CMD / Terminal

1. pip install -r requirements.txt

2. pip install opencv-python

3. pip install opencv-contrib-python


RUN USING:

python train.py

Method:

Add a member by filling form and Clicking on Take Images.

Then Train Images to train classifier.

Check using 3rd Button